jcrstart project contains an instance of JackRabbit, Apache's implementation of 
Java Content Repository (JCR), evolving from JSR-170. 
A copy of jcrstart.properties must reside in the same path as where the 
jar and .sh files are unzipped. An example jcrstart.properties is included in the archive, below are the properties that must be set prior to running the .sh/.bat. 

jcrstart.repo.local.path=[full_jcr_home_path]
jcrstart.repo.rmiserver.name=[name-within-local-server]
jcrstart.repo.rmiserver.port=[unique-port-for-name-within-local-server]
jcrstart.repo.rmiserver.username=username
jcrstart.repo.rmiserver.password=password


